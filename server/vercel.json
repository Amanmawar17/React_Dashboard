{
    "version": 2,
    "builds": [
        {
            "src": "./server/src/index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/server/src/(.*)",
            "dest": "/server/src/"
        }
    ]
  }